def convert_to_binary(number):
    binary_str = bin(number)[2:]
    binary_32_bit = binary_str.zfill(32)
    return binary_32_bit

def reverseBits(n):
    
    for i in range (binary_32_bit):
        
    pass